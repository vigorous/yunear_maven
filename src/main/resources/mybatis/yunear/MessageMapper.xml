<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MessageMapper">
	
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into YUNEAR_MESSAGE(
			MEDIA_ID,	
			TYPE,	
			CONTENT,	
			DATE_CREATE,	
			MESSAGE_ID
		) values (
			#{MEDIA_ID},	
			#{TYPE},	
			#{CONTENT},	
			#{DATE_CREATE},	
			#{MESSAGE_ID}
		)
	</insert>

	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from YUNEAR_MESSAGE
		where 
			MESSAGE_ID = #{MESSAGE_ID}
	</delete>
	
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update  YUNEAR_MESSAGE
			set 
				MEDIA_ID = #{MEDIA_ID},
				TYPE = #{TYPE},
				CONTENT = #{CONTENT},
				DATE_CREATE = #{DATE_CREATE},
				DATE_MODIFY = #{DATE_MODIFY},
				IS_DELETE = #{IS_DELETE},
			MESSAGE_ID = MESSAGE_ID
			where 
				MESSAGE_ID = #{MESSAGE_ID}
	</update>
	
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
			MEDIA_ID,	
			TYPE,	
			CONTENT,	
			DATE_CREATE,	
			DATE_MODIFY,	
			IS_DELETE,	
			MESSAGE_ID
		from 
			YUNEAR_MESSAGE
		where 
			MESSAGE_ID = #{MESSAGE_ID}
	</select>
	
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
				a.MEDIA_ID,	
				a.TYPE,	
				a.CONTENT,	
				a.DATE_CREATE,	
				a.DATE_MODIFY,	
				a.IS_DELETE,	
				a.MESSAGE_ID
		from 
				YUNEAR_MESSAGE a
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
				a.MEDIA_ID,	
				a.TYPE,	
				a.CONTENT,	
				a.DATE_CREATE,	
				a.DATE_MODIFY,	
				a.IS_DELETE,	
				a.MESSAGE_ID
		from 
				YUNEAR_MESSAGE a
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from YUNEAR_MESSAGE
		where 
			MESSAGE_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
</mapper>